name: petnote_sync_protocol
description: PetNote multi-device sync protocol shared by app and server.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.2

dependencies:
  cryptography: ^2.7.0

dev_dependencies:
  test: ^1.25.0
